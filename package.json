{
  "name": "servers-os",
  "version": "1.0.0",
  "description": "Two simple file servers (ubuntu/windows) with CRUD API and responsive UI",
  "main": "ubuntu-server.js",
  "scripts": {
    "start-ubuntu": "node ubuntu-server.js",
    "start-windows": "node windows-server.js",
    "start": "node ubuntu-server.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "express": "^4.18.2",
    "multer": "^1.4.5"
  },
  "engines": {
    "node": ">=14"
  }
}
